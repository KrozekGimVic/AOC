use crate::common::Part;

pub fn solve(data : &Vec<String>, part : Part) {

    match part {
        Part::First => {
            println!("{}", 3);
        }
        Part::Second => {
            println!("{}", 2);
        }
    }
}
