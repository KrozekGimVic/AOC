use crate::common::Part;

pub fn solve(_data : &Vec<String>, part : Part) {

    match part {
        Part::First => {
            println!("{}", 3);
        }
        Part::Second => {
            println!("{}", 2);
        }
    }
}
